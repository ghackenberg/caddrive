import * as React from 'react'

export const TagAssignmentCount = (props: { productId?: string, tagId?: string }) => {
    console.log(props)
    return (
        <>
            {'not implemented'}
        </>
    )
}