import * as React from 'react'

export const LandingView = () => {

    // RETURN

    return (
        <main className="view reduced landing">
            <button className="button fill green position center">Enter</button>
        </main>
    )

}